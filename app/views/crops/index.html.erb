<%= form_for @new_crop do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :availability %>
  <%= f.check_box :availability %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.hidden_field :farm_id, :value => params[:farm_id] %>

  <%= f.submit 'Create Crop' %>
<% end %>